# Buybacks fixtures

These files are synthetic development fixtures. They keep the static frontend buildable when `DART_API_KEY` and `KRX_AUTH_KEY` are not available. Do not treat the values as verified market data.

